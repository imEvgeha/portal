pipeline {

     stages {
         stage('build') {
             steps {
               sh 'yarn'
               sh 'yarn prebuild'
               sh 'build:local:prod'
               sh 'yarn test'
             }
         }
     }
 }
