pipeline {
    agent { label 'usla-jknd-p003' }
     stages {
         stage('build') {
             steps {
               sh 'yarn'
               sh 'yarn prebuild'
               sh 'build:local:prod'
               sh 'yarn test'
             }
         }
     }
 }
