import React from 'react';

const TitleMatchPreview = () => {
    return (
        <div>Title Matching Preview</div>
    );
};

TitleMatchPreview.propTypes = {
};

TitleMatchPreview.defaultProps = {
};

export default TitleMatchPreview;